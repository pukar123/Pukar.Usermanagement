# Copy to .env next to docker-compose.yml to override build args for ems-web.
# NEXT_PUBLIC_* is embedded at **image build** time (docker compose build).

NEXT_PUBLIC_API_BASE_URL=http://localhost:5246
NEXT_PUBLIC_DEFAULT_ORGANIZATION_ID=1
